/** Local development. League records come from labelled sample data until the API is connected. */
export const environment = {
  apiUrl: 'http://127.0.0.1:8000',
  supabaseUrl: '',
  supabasePublishableKey: '',
  sampleLeagueData: true,
};
