/**
 * Production configuration. Browser-safe values only: the API origin, the Supabase
 * project URL and its publishable key. Never put service or database secrets here.
 */
export const environment = {
  apiUrl: '',
  supabaseUrl: '',
  supabasePublishableKey: '',
  sampleLeagueData: false,
};
