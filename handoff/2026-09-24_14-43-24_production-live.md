# Production live

Recorded on 2026-09-24 in Africa/Johannesburg time. Paths are relative to the repository root. Continues `2026-09-23_23-50-59_production-deployment.md`.

## Request

After PR #1 (first production release) was merged: check the production deployment, bring `staging` level with `master`, diagnose and fix the API's failing database connection, and record the state here.

## Completed

- `staging` was fast-forwarded to `master` at `732dd1d`. Both branches now share history.
- PR #2 (`claude/db-error-diagnostics`, merged) makes the health check and snapshot cache log the database driver's reason, with URLs and `password=` fragments redacted (`describe_db_error` in `apps/api/app/db.py`). API responses are unchanged.
- That logging found two configuration faults in the Vercel Production `DATABASE_URL`, which the user fixed:
  1. The password contained `@` and `#`, which split the URL (`failed to resolve host '#@aws-0-…'`). The user set a new letters-and-digits password, because the log line had exposed one character of the old one.
  2. The password was wrapped in quotes in the URL, giving `password authentication failed for user "piele_api"`. Quotes belong only in the SQL `alter role` statement.
- Repeated failed logins trip the Supavisor circuit breaker (`ECIRCUITBREAKER … new connections are temporarily blocked`). It clears after a few minutes without attempts. Do not poll `/v1/health` while logins are failing.
- Production is now healthy: `GET https://piele-api.vercel.app/v1/health` returns `{"status":"ok","environment":"production","database":"ok","snapshotCache":"database"}`. The runtime role `piele_api` connects through `aws-0-eu-west-2.pooler.supabase.com:6543` with `sslmode=verify-full` and `certs/supabase-prod-ca-2021.crt`, so certificate verification works on Vercel.

## Environment facts

- Supabase: `piele-production` (ref `lnifzhrdvuqskwiblmqh`, eu-west-2) is active. The GitHub integration on `master` reported "All migrations are up to date" on each merge. `piele-staging` is paused for the free-plan limit of two active projects; `pofadder-bowl-league` is the other active project.
- Vercel: team `victor-4043s-projects`, team ID `team_KEXwZ0qb2Mzj7cVdAzR5Q7zF`. Projects `piele-api` (`prj_7kEaKvPYcTNUBgIKH1WDJDU1Pssp`) and `piele-web` (`prj_XyDxTCqkPbDRa3zVsNgjaKhkQwPE`). Production domains `piele-api.vercel.app` and `piele-web.vercel.app`. All Production variables from `docs/production.md` are set; `DATABASE_URL` is a Secret.
- The Vercel connector works after reconnecting with team access. Runtime log queries need a `deploymentId` or a narrow time window, otherwise they time out. Environment variable values of type Secret cannot be read.
- A Vercel redeploy can wait in `QUEUED` for several minutes. Check the deployment state before concluding that a change had no effect.

## Checks run

- `npm run smoke -- --web https://piele-web.vercel.app --api https://piele-api.vercel.app`: 15 of 16 passed. The failure is "Missing asset returns 404" (status 200).
- PR #2: `uv run pytest` gave 25 passed and 3 skipped locally; CI on the PR passed before merge.

## Open items

1. Missing static files on `piele-web` return the app shell with 200 instead of 404. The `vercel.json` rewrite excludes paths containing a dot, so the fallback probably comes from Vercel's Angular framework preset. Not yet tested; fix on a branch and check on a staging deploy.
2. `piele-staging` is paused, so the staging API reports `database: error`. Restoring it needs another project paused or a paid plan. The staging `DATABASE_URL` still uses the `postgres` role; switch it to `piele_api` (and set that role's password on staging) when it is restored.
3. Production web access: confirm whether `piele-web` production stays public or behind Vercel Authentication until sign-in exists (plan phase H2).
4. Plan phases H2 to H8 remain unstarted; see `docs/production.md` "Not yet production-ready".
